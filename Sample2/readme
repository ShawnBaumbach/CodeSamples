This is a webpage created with node.js, hmtl, css, mongo, and js.

Its purpose is to allow the user to add two data fields into the mongo database and to then retrieve all of the elements in the collection.
